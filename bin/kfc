#! /usr/bin/env node

var kfc = require('../src/index')
var args = [].slice.call(process.argv, 2)

if (args.length) {
    args.forEach(function (file) {
        var c = kfc.parse(file)
        console.log(kfc.stringify(c))
    })
}
